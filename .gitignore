node_modules/
dist/
*.log

# Environment variables — NEVER commit these (contain secrets)
.env
.env.local
.env.*.local
backend/.env
frontend/.env
**/.env
**/.env.local

# OS
.DS_Store
Thumbs.db

# Large ZIPs and uploads
backend.zip
backend_upload/

# Local persistent data — NEVER upload these to live server
# These files are auto-generated on the server and contain live data
backend/data/users.json
backend/data/progress.json
